EntregaGov mock SOAP service (Fastify + TypeScript)

Run locally:

```bash
cd entregagov_service
npm install
# development (auto-reload)
npm run dev

# or build+run
npm run build
npm start
```

Endpoint:
POST / (SOAP envelope)

Defaults to port 8002. Set PORT env var to change.
