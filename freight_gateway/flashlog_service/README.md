FlashLog mock service (Fastify + TypeScript)

Run locally:

```bash
cd flashlog_service
npm install
# development (auto-reload)
npm run dev

# or build+run
npm run build
npm start
```

Endpoint:
GET /fretes?cep=01001000

Defaults to port 8001. Set PORT env var to change.